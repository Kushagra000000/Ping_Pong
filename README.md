# Ping_Pong
A basic ping pong game, single player- Currently working on multiplayer version.

To clone in current directory:
```
    git clone --recursive-submodules <repo_link>
```

To run:
```
    cd Ping_Pong
    cmake -S . -B build
    cd build
    make
    ./pong
```